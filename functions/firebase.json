{
  "functions": {
    "source": ".",
    "runtime": "nodejs20",
    "predeploy": [
      "npm install --no-audit --no-fund",
      "npm run build"
    ]
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}